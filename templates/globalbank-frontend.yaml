apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.applications.globalbank-frontend.name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/henesi/GlobalBank-Helm-Charts/
    targetRevision: master
    path: {{ .Values.applications.globalbank-frontend.repoPath }}
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.applications.globalbank-frontend.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true